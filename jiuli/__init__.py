# -*- coding: utf-8 -*-
"""酒醴（jiuli）——瘦上下文本地 RP 引擎（v0.1 开发者预览）。

组件：检索(retriever) / 组装(context) / 状态(state) / 存档(session) /
记忆(memory) / 解析(parser) / LLM(llm) / 回合循环(loop)。
零第三方依赖；LLM 走 OpenAI 兼容接口，离线可用 MockLLM。
"""
__version__ = "0.1.0"
