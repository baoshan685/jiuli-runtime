# P2 验收报告

> 日期：2026-09-13 · 模型：glm-5.3-flash-free（reasoning）via api.teamorouter.cn
> 卡片：白城凌夜（真实世界书，7 条目/3 常驻）· 预算 4000 tok · 全部数据来自实测

## 1. 验收结论

| P2 验收项 | 结果 | 证据 |
|---|---|---|
| 50 轮单轮 ctx ≤ 4K | ✅ PASS | MockLLM 50 轮峰值 584 tok（tests/test_runtime.py::TestLongSession50）；真模型 8 轮 ctx 471→2686，真实 prompt_tokens 508→3078，随轮次亚线性增长且被预算钳制 |
| 状态零漂移 | ✅ PASS | 50 轮 MockLLM 每轮 +1，终态 50 且与 DB 完全一致（断言）；真模型会话 affection 0→1→2、mood 链正确累计落库 |
| 跨会话记忆 | ✅ PASS | 会话 B（新 session）问"还记得吗"→ mem 命中 3 条，模型自然复述会话 A 事件（"那个愚蠢的投票"） |
| 瘦上下文成本 | ✅ 达标 | 见下节，输入侧约为酒馆重度配置的 1/5～1/10 |

## 2. 真模型 8 轮实测明细

| 轮 | ctx估算 | 真实prompt | completion | 耗时 | wb命中 | mem | 状态变化 |
|---|---|---|---|---|---|---|---|
| 1 | 471 | 508 | 3731 | 42.8s | 4 | 0 | mood=羞窘·戒备 |
| 2 | 663 | 725 | 4000 | 48.8s | 0 | 3 | - |
| 3 | 833 | 932 | 6000 | 134.8s | 1 | 3 | - |
| 4 | 1414 | 1585 | 923 | 16.6s | 4 | 3 | affection+1 |
| 5 | 1705 | 1950 | 4828 | 116.7s | 2 | 3 | affection+1 |
| 6 | 2250 | 2570 | 6000 | 133.5s | 1 | 1 | - |
| 7 | 2299 | 2622 | 1149 | 78.1s | 0 | 3 | mood 被拒（>8字）✓ |
| 8 | 2686 | 3078 | 6000 | 133.7s | 0 | 3 | - |

- **ctx 估算与真实 prompt_tokens 高度吻合**（比值 ≈1.13，来自消息包裹开销）——估算器可信。
- **世界书按需命中**：字面轮 4 条命中，改写轮 1-2 条精准命中，无关轮 0 条命中——
  "该注入时注入，不该时零浪费"的行为得到验证。
- **max_len 拦截**：第 7 轮模型提交 15 字 mood，被状态机拒绝，状态保持上一值——
  "状态是字段不是自由文本"的承诺在真实模型下生效。

## 3. 成本对照（输入侧，每轮 prompt tokens）

| 方案 | 每轮输入 token（估算口径） | 说明 |
|---|---|---|
| 酒馆重度配置 | 12,000-20,000 | 全量世界书+长预设+多扩展+全量历史 |
| 酒馆轻配置 | 4,000-8,000 | 精简预设+部分世界书 |
| **酒醴实测** | **508 → 3,078（8 轮均值 ≈1,850）** | 检索命中注入+摘要+近 12 轮窗口 |

**结论：输入侧为酒馆重度配置的 1/5～1/10**，且随会话变长优势扩大
（历史增长被摘要+窗口截断，世界书按需注入）。

诚实说明两个未计入项：
1. **completion 侧很贵**：GLM-5.3 是 reasoning 模型，每轮 completion 900-6000 token
   （含思考）。这是模型特性而非运行时开销，但计费上不可忽略。缓解：换非推理模型、
   或供应商侧限制思考预算。酒馆同样付这个成本，且它的输入还更贵。
2. 对照列为行业常识估算，未实测酒馆（无安装环境）；口径已注明，欢迎实测校准。

## 4. 过程中发现并修复的缺陷

| 缺陷 | 修复 |
|---|---|
| 多线程 Web 服务器下 SQLite 连接跨线程崩溃 | check_same_thread=False + RLock 串行化 + busy_timeout |
| SSE 响应后连接挂起 | Connection: close + close_connection |
| reasoning 吃爆预算返回空正文（偶发） | max_tokens 4000 + 空正文自动重试 |
| 通用回忆提问 BM25 命中 0 | 记忆无命中时兜底注入最近 3 条事实 |

## 5. 遗留（进入 P3 待办）

- 模型偶发在 memory_facts 里虚构前史（如"说梦话提到星期三"）——需要收紧 prompt 并给事实加来源标记；
- 长会话后期（>20 轮）摘要压缩质量未压测（当前摘要由模型尾部块自愿提供，覆盖率一般）；
- 11 卡全量迁移回归待做（当前 1 真卡 + 1 合成卡）。
